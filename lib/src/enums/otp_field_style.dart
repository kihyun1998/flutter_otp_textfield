/// Defines the visual style of OTP input fields
enum OTPFieldStyle {
  /// Box-shaped field with border around all sides (default)
  box,

  /// Underline style with border only at the bottom
  underline,

  /// Circular field shape
  circle,
}
